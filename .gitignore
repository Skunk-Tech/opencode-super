# Dev/build artifacts inside the bundled harness source
opencode-harness/dist/
opencode-harness/node_modules/

# Runtime plugin state written by the installed plugin in this repo
.opencode/harness/

# Iterative execution checkpoint (per-project, gitignored)
.opencode/iteration-state.json

# Context map (regenerable structural cache, like node_modules)
graft/

# Isolated worktrees
.worktrees/
